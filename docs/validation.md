# v0.1 验证记录

验证日期：2026-09-09。

- 后端：`52 passed`。覆盖档案持久化、密钥隐藏与删除、同源保护、参数校验、资料与记忆生命周期、公网地址校验、受限平台停止、robots 规则、正文/链接提取、不可变证据快照、工具循环、引用校验、预算、记忆幂等、检查点恢复和取消。
- 测试依赖输出两条弃用提示（Starlette 的 httpx 测试客户端及 AnyIO BlockingPortal 别名），未影响测试结果。
- 浏览器：独立临时数据库与明确标记的模拟模型，验证两个 Skill 同轮加载、资料检索、结构化报告、引用查看、导出、追问、项目比较、档案保存、密钥不回显、记忆录入和脚本注入文本不执行。
- 布局：1440px 桌面与 390px 移动窗口；检查全部九个页面无横向溢出，浏览器流程无控制台错误。
- 六个 `SKILL.md` 均通过 Skill Creator 校验器。
- Python 编译和前端 JavaScript 语法检查通过。
- 本地服务监听 `127.0.0.1:8765`，生产数据库未填入模拟档案、模拟案例或测试模型配置。

真实网络采集探测已单独保存在 [source-probe.json](source-probe.json)。最近一次：CMU、UW 毕业手册入口和 GOV.UK 页面读取成功；一亩三分地连接重置；该记录为浏览器通道接入前的 HTTP 探测，小红书和 LinkedIn 当时未发起自动读取。

尚未验证：用户实际模型与 Tavily 账户、真实申请问题的答案质量、社媒登录态采集，以及长期运行下的站点稳定性。浏览器测试中的模拟模型不计作这些能力的验证。

截图位于工作区 `test-results/app-desktop.png` 和 `test-results/app-mobile.png`（真实应用首页），研究详情测试截图为 `test-results/research-desktop.png`（标记的测试数据）。

本机浏览器采集新增验证：真实 Chromium/Edge 加载拦截网络后的固定页面，覆盖两个平台的搜索卡片和正文提取、登录态检测、429 暂停、会话删除及隐私字段过滤。另有后端测试覆盖 URL 范围、每日限额、关闭状态、重启恢复和 Agent 搜索预算。界面流程验证平台配置持久化。这些测试不代表真实平台账号采集成功。

真实平台连接：更新后的本机服务已成功打开小红书与 LinkedIn 独立浏览器页面，两者返回 awaiting_login。尚未完成用户登录，不计作搜索或正文实测成功。

简历导入验证：使用生成的真实文本 PDF 验证本机提取、无模型调用和无资料入库；覆盖损坏/空白 PDF、缺少模型配置、模型格式错误、原文证据过滤和档案不被修改。浏览器使用模拟模型完成上传→原文预览→解析→已有字段默认不选→填入→保存与刷新验证。真实用户简历与实际模型解析质量尚待验证。

会话恢复验证：两个平台的浏览器夹具均覆盖保存 session Cookie、清空浏览器 Cookie 后关闭、重启状态恢复、重新采集，以及 0600 权限和清除登录态。此结果不代表平台服务器会永久接受已保存的会话。

研究收尾验证：新增不可用搜索不扣预算、已发出失败请求计数、损坏 JSON 的诚实进度摘要、收尾上下文压缩与有效引用 ID、已达上限任务只整理现有资料的回归测试。

小红书账号实测（2026-09-09）：首次使用「计算机硕士 申请经验」成功保存 8 条搜索摘要；直接读取首篇笔记失败。随后优先保留可见卡片的完整链接参数，并增加渲染等待；回归夹具通过，但真实复测出现连接/页面读取超时，正文成功尚未验证。服务重启后首页会话检查曾返回 ready，无需重新扫码。增加 `__INITIAL_STATE__` 的字段/条数诊断，不导出原始状态；真实状态路径尚未确认，不将 feed 推荐内容用作搜索结果。

小红书复测成功（2026-09-09，UTC 10:30 后）：复用已保存登录态打开首页成功；确认首页 `window.__INITIAL_STATE__.feed.feeds` 解包后有 34 条卡片，包含 id、noteCard、xsecToken 字段。用「计算机硕士 申请经验」搜索成功保存 8 条摘要，全部保留访问参数；读取其中一篇《26Fall CS美研diy总结》成功，保存 1348 字可见内容（采集器可能包含当时可见评论），无需重新扫码。结果是一次账号实测，不能保证未来持续可用；搜索页 state 的具体解包结构本次未单独确认。自动采集开关恢复测试前状态。
